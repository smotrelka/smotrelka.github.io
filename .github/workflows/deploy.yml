name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      # 3. Install dependencies
      - name: Install dependencies
        run: npm ci

      - name: Create .env file
        run: |
          echo "PUBLIC_MEILISEARCH_ENDPOINT=https://search.smotrelka.org" >> .env
          echo "PUBLIC_MEILISEARCH_APIKEY=7ea4364936aee87d8b00d9e2d139b4890e758ea83bc59ef76652b93f766cf247" >> .env

      - name: Build SvelteKit
        run: |
          npm run build

      - name: Deploy to Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build
          publish_branch: production
          force_orphan: true

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SMOTRELKA_DDC66 }}'
          channelId: live
          projectId: smotrelka-ddc66
